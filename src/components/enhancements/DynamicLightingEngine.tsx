"use client";

export function DynamicLightingEngine() {
  // CSS variables --mouse-x/y are not consumed in stylesheets;
  // avoided continuous documentElement property writes to prevent style recalculation thrashing.
  return null;
}
